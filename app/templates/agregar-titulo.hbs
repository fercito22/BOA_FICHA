<div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabelAgregar">Formulario Titulo nuevo.</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>      
      <div class="modal-body">
        <form id="miFormTituloID">
        <div class="form-group">          
          <p>Grado Formacion</p>
          <div class="input-group-prepend">
              <div class="input-group-text"><i class="fa fa-graduation-cap text-info"></i></div>              
                <select  onchange={{action 'nivelAcademicoSelected' value="target.value"}} >
                      <option disabled selected> -- Grado -- </option>
                    {{#each NivelAcademico as |itemNivel|}}
                        <option value={{itemNivel.nivelAcademicoID}} selected={{is-eq itemNivel.nivelAcademicoID formaN.nivelAcademicoID}}>{{itemNivel.Nivel}}</option>
                    {{/each}}
                </select>                   
          </div>                 
          <label hidden={{selectGrado}} class="errorMessage mensaje-error is-valid">{{mensajeGrado}}</label>
        </div>

        <div class="form-group">          
          <p>Titulo</p>
          <div class="input-group-prepend">
              <div class="input-group-text"><i class="fa fa-id-card text-info"></i></div>               
                {{input
                    value=formaN.titulo
                    placeholder="Titulo"
                    autocomplete='off'
                    class="form-control text-secondary border border-info "
                }}                 
          </div>             
          <label hidden={{nombresTitulo}} class="errorMessage mensaje-error is-valid">{{mensajeNombreTitulo}}</label>
        </div>

        <div class="form-group">          
          <p>Institucion</p>
          <div class="input-group-prepend">
              <div class="input-group-text"><i class="fa fa-university text-info"></i></div>              
                {{input
                    value=formaN.institucion
                    placeholder="Institucion"
                    autocomplete='off'
                    class="form-control text-secondary border border-info "
                }}                 
          </div>   
          <label hidden={{nombresinstitucion}} class="errorMessage mensaje-error is-valid">{{mensajeNombreInstituto}}</label>              
        </div>

        <div class="row">
            <div class="col-sm-6">
                <div class="form-group">          
                    <p>Desde</p>
                    <div class="input-group-prepend">
                        <div class="input-group-text"><i class="fa fa-calendar text-info"></i></div>                
                            {{!-- {{input
                                value=form.fechainicio
                                placeholder="Fecha Inicio"
                                autocomplete='off'
                                class="form-control text-secondary border border-info "
                            }}    --}}
                            {{pikaday-input 
                                value=formaN.fechainicio
                                format="YYYY/MM/DD" 
                                onSelection=(action 'onFechaIni') 
                                class="form-control text-secondary border border-info " 
                                placeholder="Fecha Ini" 
                            }}                                      
                    </div>     
                    <label hidden={{fechainicioTitulo}} class="errorMessage mensaje-error">{{mensajeFechaInicio}}</label>
                </div>
            </div>         

            <div class="col-sm-6">
                <div class="form-group">          
                    <p>Hasta</p>
                    <div class="input-group-prepend">
                        <div class="input-group-text"><i class="fa fa-calendar text-info"></i></div>   
                            {{pikaday-input 
                                value=formaN.fechainicio
                                format="YYYY/MM/DD" 
                                onSelection=(action 'onFechaFin') 
                                class="form-control text-secondary border border-info " 
                                placeholder="Fecha Ini" 
                            }}    
                            {{!-- disabled = isDisabledFechaInicio --}}
                            {{!-- {{pikaday-input value=formac.fechafin format="YYYY/MM/DD" onSelection=(action 'onFechaFin') class="form-control text-secondary border border-info " placeholder="Fecha Emision" }} --}}
                    </div>                      
                    <label hidden={{fechafinTitulo}} class="errorMessage mensaje-error is-valid">{{mensajeFechaFin}}</label>
                </div>                
            </div>
        </div>              

        
      </form>
      </div>

      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Cerrar</button>
        {{!-- {{#link-to 'index' tagName="button" class="btn btn-secondary"}}
            Regresar
        {{/link-to}} --}}
        {{!-- disabled={{isDisabledTitulo}} --}}
        {{!-- {{action 'onGuardarTitulo'}} --}}
        {{!-- data-dismiss="modal"  --}}
        <button hidden={{isDisabledTitulo}} data-dismiss="modal" type="button" class="btn btn-primary"  disabled={{isDisabledTitulo}}   {{action 'onGuardarTitulo'}}>Guardar</button>
        <button hidden={{ValidacionTitulo}} type="button" class="btn btn-danger"   {{action 'onGuardarTitulo'}}>Guardar</button>
      </div>
    </div>
  </div>